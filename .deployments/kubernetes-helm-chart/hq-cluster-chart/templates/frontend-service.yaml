apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      targetPort: {{ .Values.env.config.FRONTEND_PORT }}
      protocol: TCP
      name: http
  selector:
    app: frontend
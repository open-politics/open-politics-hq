apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.env.config.REDIS_PORT }}
      targetPort: {{ .Values.env.config.REDIS_PORT }}
      protocol: TCP
      name: redis
  selector:
    app: redis
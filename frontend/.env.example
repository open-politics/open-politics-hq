# Copy to .env.local and adjust as needed.
# Next.js loads .env.local automatically (gitignored).

# Set to true/1/yes to show announcements on the landing page only. Default (unset/false) = no announcements.
NEXT_PUBLIC_ANNOUNCEMENTS_ENABLED=true
